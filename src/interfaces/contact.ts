export interface Contact {
    id: number;
    firstName: string;
    lastName: string;
}

export interface AllContact {
    phone: number;
    firstName: string;
    lastName: string;
    profile_image: string;
}